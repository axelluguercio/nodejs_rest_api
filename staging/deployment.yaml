apiVersion: apps/v1
kind: Deployment
metadata:
  name: restapi-deployment
spec:
  replicas: 2 # 2 replicas
  selector:
    matchLabels:
      component: api
  template:
    metadata:
      labels:
        component: api
    spec:
      containers:
        - name: rest-api
          image: gcr.io/PROJECT_ID/api:SHA # pull image from docker registry
          ports:
            - containerPort: 4000
          env:
            - name: API_KEY
              valueFrom:
                secretKeyRef:
                  name: api-secrets
                  key: API_KEY
            - name: JWT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: api-secrets
                  key: JWT_TOKEN